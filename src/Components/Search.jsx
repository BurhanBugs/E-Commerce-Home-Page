import React from 'react'

const Search = () => {
  return (
    <div>
      hlo
    </div>
  )
}

export default Search





{/* <div key={product.id} className="Card relative flex justify-start flex-col items-center cursor-pointer border-2 hover:border-[#1F3E97] hover:bg-gray-200 h-90 shadow-sm m-6 p-3 rounded-2xl  w-[20%]">
                  <div className="image shadow-xl overflow-hidden  flex items-center justify-center  h-60 w-[80%]">
                    <img className='w-full rounded-2xl object-cover' src={product.image} alt={product.title} />
                  </div>
                  <div className="title mt-3 text-start">
                    <p className='font-semibold text-sm'>{product.title.length > 40 ? `${product.title.substring(0, 30)}...` : product.title}</p>
                  </div>
                  <div className="price mt-3">
                    <p className='font-semibold text-start text-sm'>Price: {product.price}$</p>
                  </div>
                  

                </div> */}